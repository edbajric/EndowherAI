import { PageShell } from "@/components/layout/PageShell";
import { Card } from "@/components/ui/Card";

export default function RemediesInsightsPage() {
  return (
    <PageShell
      title="Remedy effectiveness"
      subtitle="Correlations between remedies and pain reduction. (Frontend-only placeholder)"
    >
      <Card title="Analysis" description="Effect size, time windows, and confidence (placeholder)" />
    </PageShell>
  );
}
